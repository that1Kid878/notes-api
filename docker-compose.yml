services:
  app:
    image: notes-api:latest
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:/app/data/local.db
    restart: unless-stopped

volumes:
  db_data:
    driver: local
